<?php

    // Recibe el valor de postId enviado por la solicitud AJAX
    if (isset($_POST['postId'])) {
        $postId = $_POST['postId'];

        // Hacer algo con $postId, como asignarlo a una variable PHP para usarlo en tu HTML
        $postIdPHP = $postId;
    }
?>